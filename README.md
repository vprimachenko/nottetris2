# nottetris2

Runs on LÖVE 11.5.

## Releases

GitHub Actions builds release artifacts for:

- Windows x64
- macOS
- Linux x86_64
- plain `.love`

Push a tag like `v1.0.0` to build the artifacts and publish a GitHub release.
